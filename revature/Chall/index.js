data=process.argv[2];
function WordCount (data)
{
    dataArr= data.split('');
    return data.split(space).length;
}
function CharCount (data)
{
    var d= data.replace (regex, space);
    return d.split(empty).length;
}

